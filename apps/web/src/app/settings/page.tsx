import { SettingsPage } from '@/components/SettingsPage';

export const metadata = {
  title: 'Settings - Viz Notes',
  description: 'Configure your OpenRouter API key and model preferences',
};

export default function Settings() {
  return <SettingsPage />;
}
